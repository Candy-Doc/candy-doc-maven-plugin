name: Package the plugin ( Tests and build )

on:
  push:
    paths:
    - '**.java'
    - ./pom.xml

jobs:
  tests_and_build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v3
    - uses: ./.github/actions/test_and_build_plugin
